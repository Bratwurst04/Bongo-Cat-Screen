# DJ media-cat sprites

These generated sparse pixel layers are used only by the ESP32 media-cat mode.
Their editable PNG sources remain in `assets/User-made/`; regenerate
`dj_sprites.h` with `tools/pngs_to_sparse_dj_header.py` after changing them.
